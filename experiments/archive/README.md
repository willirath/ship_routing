# Archived Experiments

- **tuning_001**: Initial hyperparameter tuning experiment based on a bash loop random sampling
- **tuning_002**: Second iteration of tuning experiments with Parsl HTEX

## Note

These archived experiments will not work with the current codebase structure, as they were created before the introduction of the `ship_routing.htc` package.